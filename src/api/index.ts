import account from '@/api/endpoints/account';
import announcements from '@/api/endpoints/announcements';
import bookings from '@/api/endpoints/bookings';
import feedback from '@/api/endpoints/feedback';
import services from '@/api/endpoints/services';

export default { account, announcements, services, bookings, feedback };
